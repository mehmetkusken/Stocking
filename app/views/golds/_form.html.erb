<%= form_with(model: gold) do |form| %>
  <% if gold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gold.errors.count, "error") %> prohibited this gold from being saved:</h2>

      <ul>
        <% gold.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :buy %>
    <%= form.text_field :buy %>
  </div>

  <div class="field">
    <%= form.label :sell %>
    <%= form.text_field :sell %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
